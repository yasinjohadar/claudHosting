<?php

/**
 * Terminal Bridge — القيم الافتراضية فقط.
 * الإعدادات الفعلية تُضبط من لوحة التحكم → Coolify → Terminal Bridge.
 * يُزامَن ملف runtime تلقائياً إلى storage/app/terminal-bridge/runtime.json
 */
return [

    'runtime_file' => storage_path('app/terminal-bridge/runtime.json'),

];
